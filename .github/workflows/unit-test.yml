name: Build and Unit Test

on:
  push:
    branches: [ "unit-test-branch" ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: npm ci
      - run: npm test
